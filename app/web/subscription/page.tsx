import { SubscriptionClient } from './subscription-client'

export const metadata = {
  title: 'Dealer Subscriptions — Naija Hub Parts',
  description: 'Choose a Naija Hub Parts dealer plan. List up to 200 active products with a monthly or yearly subscription.',
}

export default function SubscriptionPage() {
  return <SubscriptionClient />
}
